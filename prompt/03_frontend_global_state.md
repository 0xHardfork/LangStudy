后端接口与数据结构已明确。你现在是 React 架构师。
请为我编写前端的入口页面 [填写文件路径，如：src/App.tsx]，暂不纠结具体气泡 UI，重点实现数据流和状态管理：

使用 [填写请求库，如：axios] 在组件挂载时请求后端的 [填写接口路径]，并将返回的数据存入状态。

在页面顶部使用 Ant Design 的 [填写具体UI组件，如：Radio.Group]，实现一个全局的状态切换控制器，用于控制 [填写状态用途，如：当前难度 Level (1/2/3)]。

预留出下方数据列表的渲染区域，通过 map 循环渲染对话列表，并准备好将数据和全局状态作为 props 传给即将编写的子组件。代码不要省略。